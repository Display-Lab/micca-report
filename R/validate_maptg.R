#`` Validate a MAPTG dataframe
validate_maptg <- function(data){
  # Column definitions are already set when reading in data.
  # Column types are already set when reading in data

  # What's left to validate?
}
